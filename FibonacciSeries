// Fibonacci series
// Time Complexity: O(n)

#include<stdio.h>

void main()
{
    int x=0, y=1, temp, i, num;
    printf("Enter length of Fibonacci series: ");
    scanf("%d",&num );
    printf("%d %d ", x, y);
    for (i=2; i<num; i++)
    {
        temp = x+y;
        printf("%d ", temp);
        x = y;
        y = temp;
    }
}