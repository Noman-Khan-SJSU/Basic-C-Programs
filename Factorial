// Find factorial of a given number
// Time Complexity: O(n)

#include<stdio.h>

void main(){

    int num, i, fact=1;
    printf("Enter the number: ");
    scanf("%d", &num);
    for (i=num; i>1; i--) {
        fact = fact * i;
        }
        printf("Factorial of %d is %d", num, fact);
}

